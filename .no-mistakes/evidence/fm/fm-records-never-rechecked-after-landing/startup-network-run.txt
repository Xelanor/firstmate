$ FM_HOME=$LAB bin/fm-startup-network.sh run --locked 0
rc=0

$ FM_HOME=$LAB bin/fm-startup-network.sh report
completed off the startup path in 3s: GitHub authentication.
QUEUED_RECHECK_INFO: STILL-TRUE RE-CHECK (queued records whose named work already landed - nothing was closed automatically):
QUEUED_RECHECK_INFO: scout-done declared report exists at data/scout-done/report.md
QUEUED_RECHECK_INFO: pr-merged named PR https://github.com/kunchenguid/firstmate/pull/5554 has merged
QUEUED_RECHECK_INFO: STILL-TRUE RE-CHECK: confirm each record is still true in two lines, or close it through the ordinary landed-work path. Nothing here removes a record.
These ran AFTER the sections above were composed, so re-read any record a line here names.
TIMINGS - where the deferred network checks spent their time (ms):
  phase       gh-auth                                    start=+111     elapsed=377
  stage       network-checks probe                       start=+51      elapsed=2856
  slowest: stage network-checks probe 2856ms, phase gh-auth 377ms

$ cat state/.wake-queue   (startup-network wake?)
